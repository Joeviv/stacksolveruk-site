---
import Navigation from './Navigation.astro'; // Import Navigation
---

<header>
  {/* Left Side: Site Logo / Title */}
  <div class="site-logo">
    <a href="/">StacksolverUK</a> {/* The StacksolverUK link on the left - UPDATED */}
  </div>

  {/* Right Side: Navigation */}
  <div class="header-nav-area">
    {/* Using the Navigation component. client:load directive was already removed as per original comment */}
    <Navigation /> 
  </div>

</header>

<style>
  header {
    background-color: #1a1a1a; /* Dark background for header */
    padding: 1rem 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap; /* Allow items to wrap on smaller screens if necessary */
    color: white;
  }

  .site-logo a { /* Style for the StacksolverUK link on the left */
      text-decoration: none;
      color: white; /* Keep white */
      font-size: 1.5rem; /* Slightly larger for better visibility */
      font-weight: bold; /* Make it bold */
  }

  .header-nav-area {
      /* Container for the Navigation component */
      /* Navigation component now handles its own internal layout/responsiveness */
  }

</style>