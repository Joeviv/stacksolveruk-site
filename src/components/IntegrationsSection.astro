---
// src/components/IntegrationsSection.astro
// De momento, no necesitamos props para este componente.
// Usaremos una imagen estática como placeholder visual.
const placeholderImageUrl = "/images/hero/collage/hero-img-1.jpg"; // Reutilizamos una imagen existente
---

<section class="integrations-section">
  <div class="section-content">
    <div class="text-content">
      <h2>Conectamos tus Sistemas, Potenciamos tu Negocio</h2>
      <p>
        En Stacksolver Tech, entendemos que la verdadera eficiencia se logra cuando todas tus
        herramientas trabajan en armonía. Nos especializamos en integrar tus aplicaciones
        existentes, desde CRMs y ERPs hasta plataformas de marketing y, crucialmente,
        soluciones personalizadas basadas en Claris FileMaker, para crear un ecosistema
        tecnológico cohesivo y potente.
      </p>
      <a href="/estrategia-digital#integraciones" class="cta-button-integrations">Descubre Más</a>
    </div>
    <div class="visual-content">
      <img src={placeholderImageUrl} alt="Visualización de integraciones tecnológicas" loading="lazy" />
    </div>
  </div>
</section>

<style>
  .integrations-section {
    background-color: #ffffff; /* Fondo blanco o un color sutil diferente */
    padding: 4rem 2rem;
    text-align: center; /* Centrado para móvil inicialmente */
  }

  .section-content {
    max-width: 1200px;
    margin: 0 auto;
    display: flex;
    flex-direction: column; /* Apilado en móvil */
    align-items: center;
    gap: 2rem;
  }

  .text-content {
    max-width: 600px; /* Ancho máximo para el texto */
    text-align: center; /* Centrado por defecto */
  }

  .text-content h2 {
    font-size: 2.5rem;
    color: #333;
    margin-bottom: 1rem;
  }

  .text-content p {
    font-size: 1.1rem;
    color: #555;
    line-height: 1.6;
    margin-bottom: 1.5rem;
  }

  .cta-button-integrations {
    display: inline-block;
    background-color: #007bff; /* Un color diferente para este CTA, o usa el mismo que el hero */
    color: white;
    padding: 0.8rem 1.8rem;
    text-decoration: none;
    border-radius: 4px;
    font-size: 1rem;
    font-weight: bold;
    transition: background-color 0.3s ease;
  }

  .cta-button-integrations:hover {
    background-color: #0056b3;
  }

  .visual-content {
    width: 100%;
    max-width: 500px; /* Tamaño máximo para la imagen/visualización */
  }

  .visual-content img {
    width: 100%;
    height: auto;
    border-radius: 8px; /* Bordes redondeados para la imagen */
    box-shadow: 0 4px 15px rgba(0,0,0,0.1);
  }

  /* Media query para escritorio (layout horizontal) */
  @media (min-width: 992px) { /* Un breakpoint un poco más grande para este layout */
    .integrations-section {
      text-align: left; /* Alinear texto a la izquierda en escritorio */
    }
    .section-content {
      flex-direction: row; /* Lado a lado */
      justify-content: space-between;
      align-items: center;
      gap: 3rem;
    }
    .text-content {
      flex: 1; /* Que el texto ocupe el espacio disponible */
      text-align: left; /* Alinear texto a la izquierda */
      max-width: none; /* Quitar restricción de ancho si es necesario para el flex */
    }
    .visual-content {
      flex: 1; /* Que el visual ocupe el espacio disponible */
      max-width: 500px; /* Mantener un tamaño máximo razonable para la imagen */
    }
  }
</style>