--- {/* Este es el bloque de frontmatter */}
// Importamos el Layout base y los componentes de la página principal
import BaseLayout from '../layouts/BaseLayout.astro';
import FeaturesGrid from '../components/FeaturesGrid.astro';
import HeroSection from '../components/HeroSection.astro';
// Quitamos: import IntegrationsSection de aquí

// Puedes añadir props aquí si la página lo necesita (ej: título específico para el <head>)
// const pageTitle = "Inicio | Stacksolver Tech";
--- {/* Cierre del frontmatter */}

{/* Usamos el BaseLayout para la estructura común (head, header, footer, etc.) */}
<BaseLayout> {/* Añade props aquí si el Layout las espera */}

  {/* Este es el contenido ÚNICO de la página de inicio. Irá al <slot /> en BaseLayout */}
  <main>

    {/* --- La Sección Hero (parte superior de la homepage) --- */}
    <HeroSection />

    {/* --- La Sección de Cuadrícula de Características (debajo del Hero) --- */}
    <FeaturesGrid />

    {/* Quitamos: La Sección de Integraciones por ahora */}
    {/* <IntegrationsSection videoUrl="..." /> */}

    {/* Puedes añadir otras secciones únicas de la homepage aquí si es necesario, debajo de las anteriores */}
    {/* <section>
        <h2>Otra Sección de la Homepage</h2>
        <p>Contenido de otra sección.</p>
    </section> */}

  </main>

  {/* Los bloques <style> y <script> en archivos .astro van fuera del Layout */}
  <style>
    /* Deja aquí los estilos CSS que solo aplican a elementos DENTRO de este <main> (o a otros elementos únicos de esta página) */
    /* Si no hay estilos específicos de esta página (que no estén en los componentes o Layout), este bloque puede estar vacío o eliminarse */
    main {
      /* Ajustes de layout para el contenido principal si no están en BaseLayout */
      margin: 0 auto; /* Centrar el contenido principal */
      padding: 2rem 1rem; /* Espacio interior para el contenido principal */
      /* Asegúrate de que el padding y margin no interfieran con los componentes hijos */
    }
  </style>

  {/* Scripts client-side específicos de esta página si los hay */}
  <script>
    // Código JavaScript que solo se necesita en esta página
    // console.log('Script de index.astro corriendo');
  </script>

</BaseLayout> {/* Cierre del BaseLayout */}